name: Demo Freeze Guard

on:
  push:
    branches:
      - demo-main

jobs:
  freeze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Demo freeze not yet implemented
        run: |
          echo "Demo freeze guard stub"
          exit 1

