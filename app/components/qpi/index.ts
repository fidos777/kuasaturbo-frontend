// ============================================================================
// QPI (QONTREK PROOF INDEX) COMPONENTS
// Sprint 3: QPI Dashboard - Real-time trust barometer by sector
// ============================================================================
// Invariant I2: "No Profit Without Proof" — Every claim must be backed
// Invariant I3: "No Scale Without Sector" — Sector context shapes trust
// Invariant I5: "No Silence Without Signal" — Every interaction produces a trace
// ============================================================================

export { default as SectorHeatmap } from './SectorHeatmap';
export { default as ProofExplorer } from './ProofExplorer';
export { default as TrustTrendChart } from './TrustTrendChart';
export { default as GovernanceActivityFeed } from './GovernanceActivityFeed';
